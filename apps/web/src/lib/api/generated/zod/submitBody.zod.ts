/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Ashyq Bilim API
 * University LMS / MOOC platform API (v2, Rust rewrite).
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod'

export const SubmitBody = zod.object({
  answers: zod
    .record(
      zod.string(),
      zod
        .union([
          zod.object({
            kind: zod.enum(['choice']),
            selected: zod.array(zod.string()).optional(),
          }),
          zod.object({
            kind: zod.enum(['open_text']),
            text: zod.string().optional(),
          }),
          zod.object({
            kind: zod.enum(['form']),
            values: zod.record(zod.string(), zod.string()).optional(),
          }),
          zod.object({
            kind: zod.enum(['code']),
            language: zod.int().describe('Judge0 language id.'),
            source: zod.string().optional(),
          }),
          zod.object({
            kind: zod.enum(['matching']),
            matches: zod
              .array(
                zod.object({
                  left: zod.string(),
                  right: zod.string(),
                }),
              )
              .optional(),
          }),
        ])
        .describe(
          'Internally tagged on `kind`, mirroring the item body kinds. Unknown\nfields are refused (UX-108: a `pairs` matching answer used to be\naccepted and stored empty).',
        ),
    )
    .nullish()
    .describe('A last patch applied before grading.'),
  violation_count: zod
    .int()
    .optional()
    .describe("The client's anti-cheat count; the server's own count wins when higher."),
})

export type SubmitBody = zod.input<typeof SubmitBody>
export type SubmitBodyOutput = zod.output<typeof SubmitBody>
