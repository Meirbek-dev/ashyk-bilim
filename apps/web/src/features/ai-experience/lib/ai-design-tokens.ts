export const AI_DENSITY = {
  timelineGap: 'gap-2',
  panelRadius: 'rounded-lg',
  compactPadding: 'p-3',
  comfortablePadding: 'p-4',
}

export const AI_MOTION = {
  fastMs: 150,
  normalMs: 220,
  slowMs: 300,
}

export const AI_CHART_SEMANTICS = {
  confident: 'hsl(var(--primary))',
  warning: 'hsl(var(--warning, var(--primary)))',
  neutral: 'hsl(var(--muted-foreground))',
}
