# Gauntlet Ledger — v2 browser QA

Durable state for the gauntlet loop (see the loop brief). Resume from this file.

## Setup facts

- Branch `rewrite`, direct commits. `apps/api` is read-only reference.
- Build: `$env:CARGO_TARGET_DIR='C:\cargo-target\ashyq-server'` in every shell.
- Podman stack (network `ashyq-dev`): `ashyq-test-pg` :5433, `ashyq-test-redis` :6380,
  `ashyq-zitadel` :8081, `ashyq-rustfs` :9002 (buckets `ab-public`, `ab-private`).
  All are `--rm`: gone after a machine restart, recreate per `apps/server/AGENTS.md`.
- DB in use: **`ashyq_dev`** on the test-pg container (20 migrations). The P10
  rehearsal DB `ashyq_dev_p10` did **not** survive the restart (ephemeral container),
  and the legacy restore in `temp-restore/` is real user data — not re-imported.
  Content is created through the v2 UI during probing instead.
- Env for the server: `scratchpad/abenv.sh` (AB__* adapted from
  `docker-compose.cutover.yml` to localhost). Zitadel PAT from
  `%TEMP%\zitadel-machinekey\pat.txt`.
- Server: `ashyq serve` on :8000, `GET /api/v2/health` → `{"status":"ok"}`.
- **Worker is a separate process**: `ashyq worker` (same env) runs the job queue —
  `ai:execute_run`, `submissions:auto-submit`, `assessments:publish-due`,
  `grading:bulk-action`, `analytics:rollup`, `uploads:reap`. Without it queued AI runs
  sit in `queued` forever and deadline auto-submits never fire (passes 1–4 ran without
  it; started 2026-09-12, log in `scratchpad/worker.log`).
- **Judge0 is not part of the local stack**: `GET code/languages` → 503, so code
  challenges cannot be authored or run (F14). The e2e code tests skip on that probe.
- **RustFS needs bucket CORS** for the browser's presigned PUT (`extra/storage-cors.json`,
  applied to `ab-private`/`ab-public`); curl uploads worked without it, browser ones did not.
- **Stack rebuilt 2026-09-12** (containers were `--rm`): fresh `ashyq_dev`, new Zitadel —
  `abenv.sh` reads the PAT from `$TEMP/zitadel-machinekey/pat.txt` and carries
  `AB__SERVER__WEB_URL=http://localhost:3000`; the three accounts were recreated (same
  logins, new ids); no seeded «Основы Python» — content is created by the e2e suite and
  critics. Self-registration exists now (`/ru/auth/signup`).
- **Two-session hazard**: a Playwright login revokes a curl session of the same user
  (single-session semantics) — mint one or the other, not both.
- **`just openapi` truncates the file when the server binary is running** — always
  `taskkill //F //IM ashyq.exe` first (bit us twice).
- **Next 16 keeps the previous route segment in a hidden `<Activity>`** — CSS locators
  like `input[name=…]` match the stale form too; scope to `form:visible`.
- **Cargo target hygiene**: `C:\cargo-target\ashyq-server` reached 135 GB (test-binary
  hashes); `cargo clean` + rebuild when C: runs low. Build test binaries with
  `--build-jobs 2` while anything else heavy runs.
- **Playwright**: `apps/web/e2e/.env.test.local` (gitignored) carries the three accounts
  and `E2E_API_URL=http://127.0.0.1:8000/api/v2` (Node resolves `localhost` to `::1`,
  which the server does not listen on). `bun run test:e2e` runs chromium for everything
  plus firefox/webkit/mobile-chrome smoke; `bunx playwright install firefox webkit` once.
  Concurrent Playwright runs wipe each other's `e2e/.auth` — run one at a time.
- URL locale prefixes are `/ru` (ru-RU, default), **`/kz`** (kk-KZ — country code, not `kk`) and `/en` (en-US), per `apps/web/src/i18n/config.ts`. A wrong prefix is not rejected: `/ru-RU/x` and `/kk/x` 307 to `/ru/ru-RU/x` and `/ru/kk/x`, which then render as unknown routes.
- Web: `apps/web/.env.local` points NEXT_PUBLIC_API_URL / INTERNAL_API_URL at
  `http://localhost:8000/api/v2/`. Launch with `preview_start {name:"web"}` (:3000).
  The `api` launch.json entry is the legacy Python app — never use it.
- **Test accounts** (password `GauntletDev!2026`, login by email):
  | role | login | username | app role |
  |---|---|---|---|
  | learner | learner@ashyq.local | learner | `user` |
  | teacher | teacher@ashyq.local | teacher | `instructor` |
  | admin | admin@ashyq.local | admin | `admin` |
  Created via Zitadel `POST /v2/users/human` + rows in `users`/`user_roles`. Zitadel ids change on every stack rebuild — recreate all three each time.
  There is no v2 user-creation endpoint (see BUG-001), so recreate them this way.
- **Observing API traffic: watch the SERVER log, not the browser.** Almost every v2
  call is made server-side by Next (server components / the BFF), so the browser's
  network panel shows only `localhost:3000` traffic. Only genuinely client-side
  TanStack Query calls appear as cross-origin requests to `:8000`.
- **Browser tooling quirk:** in this pane `javascript_tool` and `get_page_text` evaluate
  against a stale detached document — they report an empty body, `display:none` roots and
  no React fibers even when the page renders correctly. Trust `read_page`, `find` and
  screenshots; do not diagnose from `javascript_tool`.
- `read_page` with `filter:"interactive"` often returns "(empty page)"; use `filter:"all"`.
  Do not emulate a viewport with `resize_window` — a custom size desynchronises the
  click coordinate frame from the pane and every `left_click` by ref fails with
  "outside the viewport". Use `preset:"desktop"`.
- Fable's rate limit was exhausted early in pass 1; builder subagents ran on Sonnet.
- **Server test suite:** `cargo nextest run --workspace --build-jobs 4` still crashed
  rustc with `STATUS_STACK_BUFFER_OVERRUN` while compiling `work_queue_flow` when three
  subagents and the dev server were also running — the page-file hazard AGENTS.md warns
  about. Run it with nothing else heavy running, and drop to `--build-jobs 2` if it
  recurs. `just check` (fmt + clippy + sqlx offline) is cheap and was green.
- **Seeding via curl from Git Bash mangles Cyrillic to `?`** — seed content through the
  UI, or fix names afterwards with `podman exec -i … psql` (which handles UTF-8 fine).
- **Reliable clicking in this pane:** stay at the pane's native size, take a screenshot,
  and click by COORDINATE from that screenshot's stated frame. `find`/`read_page` refs
  are fine for `form_input`, unreliable for `left_click`.
- **Media locally:** apply `extra/storage-public-policy.json` to `ab-public` once after
  creating the bucket (aws-cli, see the stack-up commands), set
  `CONTENT_REWRITE_TARGET=http://localhost:9002/ab-public` and
  `NEXT_PUBLIC_MEDIA_URL=http://localhost:3000/` in `apps/web/.env.local` — otherwise
  avatars and block media 404 (ORB) because nothing serves `/content/`.
- **Pass 11+ process layout (2026-09-13):** `abenv.sh` and the live logs sit in the
  session scratchpad `%LOCALAPPDATA%\Temp\claude\X--projects-ashyq-bilim\a3c95abb-74bf-437e-98f7-358220713b12\scratchpad\`
  (`abenv.sh`, `server.log`, `worker.log`). Serve + worker run with
  `RUST_LOG='info,tower_http::trace=debug'` so every request logs
  `method/uri → status/latency` — that is the per-request evidence line for critics.
- **Session cap, not single-session:** `MAX_SESSIONS_PER_USER = 10`, oldest evicted
  (`identity/sessions.rs`). A curl session dies after ~10 later logins of the same user
  (every Playwright run logs each role in), and an RBAC change on a user revokes ALL
  their sessions (`rbac_admin.rs:290`). Mint curl sessions right before use, re-mint on
  401, and never change the roles of the three shared accounts — create your own user.
- `psql` needs `podman exec -i` (without `-i` the heredoc is silently dropped).

## v1→v2 client drift map (pass 1 survey, 2026-09-10)

The P9 frontend adaptation is incomplete: `apps/web` still calls legacy v1 URL
shapes in many services. Generated by diffing every `apiJson`/`apiResult`/`apiFetch`
literal in `apps/web/src` against `apps/server/openapi.v2.json` (168 routes).
**Candidate mappings — each builder must confirm the target against
`openapi.v2.json` (request DTO, params, response shape) before editing.**
Cursor pagination (`{items, next_cursor}`) replaces `page/limit` + `x-total-count`
everywhere; call sites that read `x-total-count` need rework, not a URL swap.

| web call site | legacy shape called | v2 target |
|---|---|---|
| `services/courses/courses.ts:257` | `courses/page/{p}/limit/{l}` | `GET courses?cursor=&limit=` |
| `services/courses/courses.ts:299,551`, `hooks/courses/courseKeys.ts:37` | `courses/editable/page/...` | **no v2 route** — needs a server route or a DECISIONS entry |
| `services/courses/collections.ts:53` | `collections/page/1/limit/20` | `GET collections?limit=` |
| `services/courses/activities.ts:42` | `POST activities/` | `POST chapters/{id}/activities` |
| `services/courses/chapters.ts:39` | `POST chapters` | `POST courses/{id}/chapters` |
| `services/courses/chapters.ts:25` | `chapters/course/{c}/order` | `POST chapters/{id}/move` |
| `services/courses/courses.ts:338,353,390,406,448,496` | `courses/{c}/rights,meta,metadata,access,readiness,thumbnail` | mostly **no v2 route**; `GET/PATCH courses/{id}` covers meta/metadata |
| `services/courses/courses.ts:580,593,611,629` | `courses/{c}/contributors*` | **no v2 route** (collaboration surface) |
| `services/courses/updates.ts:20` | `courses/{c}/update/{u}` | `PATCH/DELETE course-updates/{id}` |
| `services/assessments/assessments.ts:54`, `features/courses/queries/course.query.ts:175`, `features/assessments/studio/context.tsx:53`, `features/assessments/review/AssessmentReviewWorkspace.tsx:51`, `services/courses/code-challenges.ts:257` | `assessments/activity/{a}` | `GET activities/{id}/assessment` |
| `features/assessments/studio/components/NativeItemAuthor.tsx:151,245`, `features/assessments/studio/tabs/BuilderCanvasTab.tsx:219,300` | `assessments/{a}/items/{i}` | `PATCH/DELETE assessment-items/{id}` |
| `features/assessments/studio/components/NativeItemAuthor.tsx:223` | `assessments/{a}/items:reorder` | `POST assessments/{id}/items/reorder` |
| `services/assessments/assessment-actions.ts:292`, `services/courses/code-challenges.ts:590,636` | `assessments/{a}/items/{i}/runs` | `POST assessment-items/{id}/runs` |
| `features/assessments/shell/InlineAssessmentWorkspace.tsx:97` | `assessments/{a}/start` | `POST assessments/{id}/submissions` |
| `services/courses/code-challenges.ts:555` | `assessments/{a}/submit` | `POST submissions/{id}/submit` |
| `services/courses/code-challenges.ts:686` | `assessments/{a}/me` | `GET assessments/{id}/submissions/me` |
| `features/grading/queries/grading.query.ts:53`, `services/grading/grading.ts:14` | `assessments/{a}/submissions/{s}` | `GET submissions/{id}` |
| `services/assessments/assessment-actions.ts:261` | `assessments/{a}/submissions/{s}/grade` | `PATCH submissions/{id}/grade` |
| `features/code-arena/authoring/ReferenceSolutionRunner.tsx:56` | `assessments/{a}/code-challenge/validate` | `POST assessments/{id}/reference-check` |
| `features/assessments/registry/exam/queries.ts:10` | `assessments/exam/config` | **no v2 route** |
| `services/assessments/assessment-actions.ts:191` | `assessments/policy-preset/{kind}` | **no v2 route**; `PUT assessments/{id}/policy` exists |
| `features/courses/queries/course.query.ts:145` | `certifications/user/all` | `GET me/certificates` |
| `features/courses/queries/course.query.ts:152` | `certifications/user/course/{c}` | `GET courses/{id}/certificates/me` |
| `features/courses/queries/course.query.ts:159`, `services/courses/certifications.ts:77` | `certifications/certificate/{c}` | `GET certifications/{id}` / `GET certificates/{code}` |
| `services/courses/code-challenges.ts:252` | `code-execution/languages` | `GET code/languages` |
| `features/file-submissions/services/file-submissions.ts:95` | `file-submissions/activity/{a}` | `GET activities/{id}/file-submission` |
| `features/file-submissions/services/file-submissions.ts:257,289` | `file-submissions/{f}/submissions/{att}[/grade]` | `GET file-submission-attempts/{id}`, `PATCH .../grade` |
| `features/file-submissions/services/file-submissions.ts:266` | `file-submissions/files/{f}/url` | `GET file-submission-files/{id}/url` |
| `features/grading/queries/grading.query.ts:74` | `grading/courses/{c}/gradebook` | `GET courses/{id}/gradebook` |
| `app/[locale]/(platform)/dash/page.tsx:317,327` | `me/work` | `GET work` |
| `services/courses/activity.ts:11,22,33,44` | `trail/add_course/{c}`, `trail/remove_course/{c}`, `trail/add_activity/{a}`, `trail/remove_activity/{a}` | `POST/DELETE trail/courses/{id}`, `POST/DELETE trail/activities/{id}` |
| `services/utils/chunked-upload.ts:95,116,145` | `uploads/initiate`, `uploads/chunk`, `uploads/complete` | `POST uploads`, `POST uploads/{id}/finalize` (no chunk route — presigned PUT) |
| `services/usergroups/usergroups.ts:20,32` | `usergroups/{g}/add_users`, `remove_users` | `POST/DELETE usergroups/{id}/members` |
| `services/usergroups/usergroups.ts:92,106` | `usergroups/{g}/add_resources`, `remove_resources` | `POST/DELETE usergroups/{id}/courses` |
| `features/users/queries/users.query.ts:40` | `usergroups/{g}/users` | `GET usergroups/{id}/members` |
| `services/users/users.ts:20,24` | `users/id/{id}`, `users/username/{u}` | `GET users` (list + filter) |
| `services/users/users.ts:34,46,57` | `users/update_avatar/{id}`, `users/preferences/theme/{id}`, `users/preferences/locale/{id}` | `PATCH users/me` |
| `services/users/users.ts:28` | `users/{id}/courses` | **no v2 route** |
| `services/courses/activities.ts:136` | `utils/link-preview` | **no v2 route** |

Not drift (dynamic prefixes / test fixtures, verified fine): `${courseEndpoints.detail(x)}/discussions`,
`${courseEndpoints.detail(x)}/updates`, `analytics/${path}`, `${path}` in
`grading.query.ts:97`, `/api/log-error` (a Next route handler), and every literal in
`src/tests/lib/api-client.test.ts`.

Regenerate this survey with `scratchpad/drift.py`.

## Pass log

| pass | date | features probed | bugs found/fixed | gate | commit |
|---|---|---|---|---|---|
| 11 | 2026-09-13 | **in progress** — full sweep, scopes A+B+C on every non-blocked row (F01–F57 minus F14) in six clusters: identity, learner catalog/course, learner assessments, teacher authoring, teacher grading/AI, analytics/admin; stale rows first (F08 F47 F54 from 0946a2a) | — | — | — |
| 0 | 2026-09-10 | setup only: stack, accounts, ledger | — | — | e654349 |
| 10 | 2026-09-13 | confirmation critic on the five pass-9 fails (R1–R10): all pass; two late gaps fixed (reporter learner-state 403, link-preview refetch on learner view) | BUG-089..090 fixed | e2e 92/2 skipped (07 string fixed), vitest 681/681, typecheck 0, error-codes 32/32; nextest 319/319, `just check` green | 0946a2a |
| 9 | 2026-09-12/13 | **contract phase** — owner answered every QUESTIONS item (DECISIONS "Owner answers"): wave 1 (identity, catalog, grading) + wave 2 (analytics/jobs/ETL, certificates/AI/link-preview, RBAC/kk polyfill/localization sweep) landed F41–F55; three critics re-drove the new features + a learner regression sweep (24/29 pass); three polish builders closed the fails and ~40 nits | BUG-084..088 found and fixed | e2e 92/2 skipped, vitest 681/681, typecheck 0, error-codes 32/32; nextest 319/319, deny/machete green, CI green (34683118190) | 6167ef4 |
| 8 | 2026-09-12 | final critic load of the four pass-7 fails (F21 F26 F31 F32): all pass, zero console/page errors across 14 analytics loads and 5 studio loads; three wording nits fixed | 4 nits → fixed | `bun run test:e2e` **86 passed / 2 skipped** (third green run on the final tree); web typecheck 0, vitest 626/626, error-codes 25/25; server nextest 283/283, `just check` green | 2a03ccc |
| 7 | 2026-09-12 | two critics re-drove the 19 pass-6 fails: 15 pass, 4 fail (F21 console error, F26 hollow file cells, F31/F32 kz copy) + 9 new gaps; server: leave-course resets completions (DECISIONS), stale projection rows repaired with `ashyq admin progress-backfill`, 5 locally corrupted grades re-graded through the API; two builders closed the 4 fails and the gaps | BUG-077..083 found, all fixed | web typecheck 0, vitest 626/626, error-codes 25/25, `bun run test:e2e` 86/2 skipped (see pass log below for the final run); server `just check` green, nextest 283/283 | 1b1a3ad |
| 6 | 2026-09-12 | three critics swept ALL 40 rows with headless Playwright (learner F05–F18/F30/F35, teacher F19–F37, admin F01–F04/F38–F40): 21 pass, 18 fail, F14 blocked; server: private-course visibility regression (every learner saw every draft), TOTP 409, thumbnail upload contract, hidden unreleased grades, revision-attempt cap, item-score guard; three builders closed the 18 fails; QUESTIONS Q-2026-09-12-2 (11 contract gaps) | BUG-059..076 found, 17 fixed, 1 blocked | web typecheck 0, vitest 620/620, error-codes 25/25, `bun run test:e2e` **86 passed / 2 skipped** (twice: 85+3, 86+2); server `just check` green, nextest **283/283**, CI green (34660958702) | f95697b |
| 5 | 2026-09-12 | Playwright suite brought onto v2 (00–08 all specs; 03→06 serial chain green except the Judge0-gated code test); builders: AI panels v2, learner/account batch, teacher course list, admin users/roles (F38), users-settings gating, authoring/exam/grading gaps from the chain; critic C1–C11 (8 pass, 3 fail → fixed) + 12 new gaps (10 fixed, 2 noted) | BUG-034..044 fixed; BUG-045..058 found, 12 fixed | web typecheck **0**, vitest 583/583, error-codes 25/25; e2e chromium 41/42 (+21 cross-browser smoke); server `just check` + deny/machete/sqlx-check green, nextest 281/281, CI green (34645681889) | 2fef2c8 |
| 4 | 2026-09-12 | critic re-drove all pass-3 fixes (A1–A10: 9 pass, A7 fail on missing catalog keys that a concurrent builder then added) and first-probed F02 F11 F12 F16 F17 F18 F35 F40 (B1–B8: 4 pass, 4 fail); builders converted AI experience + Q&A + code arena, audited discussions/gamification/account, redacted the learner answer key (server), fixed local media serving | 12 gaps → BUG-034..044 | web vitest 538/538, typecheck 10; server `just check` green | 02b266e |
| 3 | 2026-09-11/12 | critic re-drove F01/F04/F06/F07/F08/F09/F13/F19/F23/F25/F26/F30/F38 from fresh sessions: 5 pass (F04 F07 F19 F30, F09 happy path), 8 fail with 19 concrete gaps → BUG-023..033; builders fixed UX-013/014 and the editor block uploads | 19 gaps found, all addressed by 3 builders (2 not reproduced: dropdown z-index, attempt highlight) | server `just check` green; web typecheck 10 (admin pages), vitest **518/518**, error-codes 25/25 | ce4ad41 |
| 2 | 2026-09-11 | teacher authoring (F20 add activity, F23 studio + publish, F19 course publish, F24 override), learner path (F08 enrol, F13 quiz, F10 activity page), teacher grading (F25 review + publish, F26 gradebook), seam smoke learner→teacher→learner; code-only conversions of uploads (F22) and file submissions (F15); i18n sweep of the attempt surface | 9 bugs found, 9 fixed; 5 UX rows, 4 fixed | server: `just check` green, nextest 281/281; web: typecheck 10 (admin pages), vitest 484/484, error-codes 25/25 | f510686 |
| 1 | 2026-09-10/11 | auth (F01–F06), home (F07), collections (F09), trail (F11), search (F30), analytics (F31/F34), admin users (F38), course create + workspace (F19), plus the whole v1→v2 client drift survey | 15 bugs found, 10 fixed; 6 UX rows; 7 contract gaps to QUESTIONS.md | server: `just check` green, nextest **281/281**. web: typecheck 80 → 10 (all 10 in the contract-blocked admin pages, Q-6), vitest **467/467**, check:error-codes 25/25, `next build` prerenders all 208 pages. CI green: https://github.com/Meirbek-dev/ashyk-bilim/actions/runs/34619423463 | 6b1d2de+ |

## Features

Status: `pass` = verified by a critic from a fresh session after the last fix touching the row; `blocked` = needs infrastructure the local stack lacks.

| id | area | route(s) | roles | status | last critic verdict |
|---|---|---|---|---|---|
| F01 | login + session | `/ru/auth/login` | all | pass | pass 7 (learner7): logged-out settings page → login with returnTo; signed-in `/auth/login` and `/login` redirect away; pass 6 happy paths |
| F02 | MFA (TOTP enroll/verify/remove) | `/ru/dash/user-account/settings/security` | any | pass | pass 7: enrol → verify → TOTP login → 409 on re-enrol flips to enrolled with «Отключить» → disable → login without TOTP (Q-2026-09-10-7 stays: no enrolment flag on the wire) |
| F03 | Google login intent | `/ru/auth/login` → `/api/v2/auth/google` | any | pass | pass 6: Google → `/api/v2/auth/google` → 303 with `?error=service-unavailable`; host-relative redirect is correct behind the production nginx (Q-2026-09-12-2 #10) |
| F04 | sessions list + revoke + logout | `/ru/dash/user-account/settings/security` | any | pass | pass 6 admin critic: two sessions listed with device labels + Russian dates, revoke removes the row without reload and bounces the other context |
| F05 | unauthorized / error pages | `/ru/unauthorized`, any bad path | all | pass | pass 6 learner critic: unauthorized + 404 localized and titled in ru/kz, back link works |
| F06 | locale switch ru/kz/en | all routes | any | pass | pass 7: kz titles on dash/settings/usergroups, no leakage; certificate type localized. Chromium builds without kk ICU render dates as «2026 M09 12» (Q-2026-09-12-2 #11) — hydration leaves now `suppressHydrationWarning` |
| F07 | home / landing | `/[locale]` | learner | pass | pass 6 learner critic: sections, 20 cards, pagination, collections; all hrefs 2xx; edge control now disabled (f95697b) |
| F08 | course browse + enroll (learner enrolled; "Курс успешно начат") | `/ru/courses`, `/ru/course/[id]` | learner | pass | pass 7: catalogue lists only accessible courses; enrol toasts and flips; leave confirms + toasts (and now resets progress, 581a06c) |
| F09 | collections | `/ru/collections`, `/ru/collections/new`, `/ru/collection/[id]` | learner+teacher | pass | pass 3 critic: create/list/open collections, localized empty states |
| F10 | activity viewer | `/course/[uuid]/activity/[id]` | learner | pass | pass 6 learner critic: blocks render, mark complete updates sidebar without reload, sidebar navigation |
| F11 | trail / progress | `/trail` | learner | pass | pass 6 learner critic: trail N/M and % agree with course pages; leave-course confirm + toast added f95697b |
| F12 | user profile | `/ru/user/[username]` | learner | pass | pass 7: profile with authored courses, app-suffixed title, no 4xx |
| F13 | taking a quiz/exam | `/ru/course/[id]/activity/[id]` | learner | pass | pass 7: retake with no false banners, submit refreshes sidebar/badge without reload, no draft 404; unreleased exams showed stale `failed/passed` from rows projected before 68c1af0 → `ashyq admin progress-backfill` rewrote them (`graded_hidden`, score null); learner sees «Оценка выполняется» |
| F14 | code arena run | `/assessments/[uuid]` (code item) | learner | blocked | Judge0 not in the local stack (`code/languages` 503); e2e code tests skip on the probe. Needs an environment with the executor |
| F15 | file submission upload | `/assessments/[uuid]` (file item) | learner | pass | pass 7: unconfigured activity → «Задание ещё не настроено»; configured ones show submitted/graded states; e2e 04 uploads a PDF |
| F16 | discussions | course discussion surfaces | learner+teacher | pass | pass 6 learner critic: post/reply/like/edit/delete, counters live, no false edited marker |
| F17 | certificates + verify | `/certificates/[uuid]/verify`, `/dash/courses/[uuid]/certificate` | any | pass | pass 6 learner critic: trail certificate list, verify page valid/not-found; control relabelled (no download route) |
| F18 | gamification | `/dash/user-account/settings/gamification` | learner | pass | pass 6 learner critic: toggles persist, units «ОП» everywhere |
| F19 | course create + details + publish | `/ru/dash/courses/new`, `/ru/dash/courses/[id]/{details,review}` | teacher | pass | pass 7 (teacher7): create → clean details → rename/description save → thumbnail via uploads (webp 200 on cards/landing) → publish → in catalogue |
| F20 | curriculum (chapters/activities) | `/ru/dash/courses/[id]/curriculum` | teacher | pass | pass 7: chapter/activity create/rename/publish/reorder/delete each toast and persist; exam dialog fires no request |
| F21 | activity editor (blocks) | `/editor/course/[id]/activity/[uuid]/edit` | teacher | pass | pass 8 (teacher8): image via real click, 5 studio loads + learner view with ZERO console errors, autosave persists |
| F22 | uploads (images/video/docs) | editor + course thumbnails | teacher | pass | pass 7: block image and course thumbnail served 200 image/webp on studio, learner view, cards, landing |
| F23 | assessment authoring | `/ru/dash/courses/[id]/activity/[id]/studio` | teacher | pass | pass 6 teacher critic: 4 item kinds, autosave, readiness, publish |
| F24 | assessment access + overrides | `/ru/dash/courses/[id]/access` | teacher | pass | pass 6 teacher critic: restricted access + override CRUD persisted |
| F25 | submission grading + item feedback | `/ru/dash/courses/[id]/activity/[id]/review` | teacher | pass | pass 7: 33.33/0/20 stored as entered, total 53.33/99.99, publish → learner sees 53.34 %; re-grade + re-publish; file chips Russian |
| F26 | gradebook + bulk actions | `/ru/dash/courses/[id]/gradebook` | teacher | pass | pass 8: 77 % published file cell, CSV row `…,33.33,Не начато,77`, «Файл» label, summaries, cell → file review; export now toasts (a2f) |
| F27 | grading SSE live updates | gradebook / review | teacher | pass | pass 7: API grade change in a second context → open gradebook cell updated at +11.8 s without reload (polling; no stream, Q-2026-09-12-2 #2) |
| F28 | work queue | `/dash/courses/[uuid]/review` | teacher | pass | pass 6 teacher critic: queue item links to a real page; tool cards real |
| F29 | course collaboration/access | `/dash/courses/[uuid]/collaboration` | teacher | pass | pass 7: collaboration → access, honest unavailable state, no request, no raw error; contradictory intro sentence dropped (1b1a3ad) |
| F30 | search | `/ru/search?q=` | any | pass | pass 6 learner critic: facets, results, navigation |
| F31 | analytics: overview/performance/operations | `/ru/dash/analytics/*` | teacher+admin | pass | pass 8: ru+kz overview/performance/operations/courses/assessments/watchlist/at-risk — zero page errors, Kazakh tile definitions, no backlog/cutover, real titles |
| F32 | analytics: courses + assessments drilldown | `/dash/analytics/courses/*`, `/assessments/*` | teacher | pass | pass 8: course + exam drilldowns ru/kz clean; status «Опубликовано/Жарияланды»; chart tick wrapping is content, not a label |
| F33 | analytics: at-risk + watchlist | `/ru/dash/analytics/watchlist` | teacher | pass | pass 7: dialog 768×539 in viewport, Russian/Kazakh throughout, action logged, row updated without reload |
| F34 | analytics CSV export | `/ru/dash/analytics/*` | teacher | pass | pass 6 teacher critic: exports 200 text/csv with headers |
| F35 | AI agent: QA / remediation (SSE) | learner surfaces | learner | pass | pass 6 learner critic: Q&A reaches a definite localized draft-mode state, delete toasts |
| F36 | AI agent: lecture authoring critique | `/dash/courses/[uuid]` | teacher | pass | pass 6 teacher critic: course analysis completes (worker running), score published |
| F37 | AI agents: remaining 4 | assorted | teacher | pass | pass 7: submission analysis → definite localized state, feedback draft fills the editor, remediation queued; course analysis findings localized, publish toasts |
| F38 | admin surface | `/dash/admin`, `/dash/admin/users`, `/dash/admin/roles` | admin | pass | pass 5 + pass 6 admin critic: users/roles CRUD with toasts and live tables, kz clean, own-row protections |
| F39 | usergroups | `/dash/users/settings/usergroups` | admin | pass | pass 7: teacher picker via /search (no 403), one action per row, read-only foreign groups, no hydration error, CRUD toasts for both roles |
| F40 | user account general/profile settings | `/dash/user-account/settings/{general,profile}` | any | pass | pass 6 admin critic: rename/avatar/security dates; password change is not in v2 by design (AGENTS.md) |
| F41 | self-registration + email verification | `/[locale]/auth/signup`, `/auth/verify-email` | anonymous | pass | pass 9 (auth9 N1): field errors, taken email/username inline, happy path → toast + login (ru/kz), login by email and username; verify page wrong code inline. e2e 00/01 |
| F42 | password change + MFA state on load | `/dash/user-account/settings/security` | any | pass | pass 9 N2/N3: wrong current password inline (no logout), change → toast, `mfa_enabled` renders the enrolled state after reload, disable → login without TOTP; pass 10 R9: otpauth issuer «Ashyq Bilim», revoke asks for confirmation |
| F43 | admin user creation | `/dash/admin/users` | admin | pass | pass 9 N4: dialog → toast → row without reload, new user logs in, taken username inline |
| F44 | RBAC codes + custom role text | `/dash/admin/{users,roles}` | admin | pass | pass 9 N5: `last-admin`, `self-disable`, `role-slug-taken` localized; custom role display name/description shown as text, edit/delete |
| F45 | request_id + web-origin redirects | problem+json, Google error | any | pass | pass 9 N6/N7: body `request_id` = header; Google error lands on `http://localhost:3000/ru/auth/login?error=…` with a localized banner |
| F46 | teacher course list (server-side) | `/dash/courses?mine…` | teacher | pass | pass 9 T1: presets/search/sort/pagination, summary counts match |
| F47 | course collaboration | `/dash/courses/[id]/collaboration`, landing apply | teacher+learner | pass | pass 10 R1: add via search → maintainer edits the curriculum with a `user` role, reporter reads only, remove; apply → pending → approve (pass 9 T2) |
| F48 | course readiness (server) + file-submission publish gate | review page, curriculum toggle | teacher | pass | pass 10 R2: blockers link to their stage, `activity-not-ready` toast localized, no 500 |
| F49 | gradebook: file cells, CSV export, live stream | `/dash/courses/[id]/gradebook` | teacher | pass | pass 9 T4: file cells from the wire, server CSV with ru header, grade published elsewhere updates the cell in 316 ms via `grading/events`; pass 10 R10 |
| F50 | grader feedback codes + learner verdicts | review, result card | teacher+learner | pass | pass 10 R3: «Верно / Ответ не дан / Совпало пар: 2/3» on both sides, one number format |
| F51 | matching items (learner body) | quiz attempt + review | learner+teacher | pass | pass 10 R4: two visible columns, shuffled right column, review shows the pairing without raw keys |
| F52 | certificate PDF | `/trail`, verify page | learner | pass | pass 9 T10 + learner9 F17: `certificate-<code>.pdf` (18 KB, `%PDF`), toast, kz label |
| F53 | AI on file-submission attempts | file review page | teacher | pass | pass 9 T8: analysis → definite draft-mode state, feedback draft, remediation card; pass 10 R10 «черновой режим» |
| F54 | link preview block | lecture editor | teacher | pass | pass 10 R6: one request per URL, inline scheme validation, safe fallback; learner view never refetches |
| F55 | analytics codes + retention | `/dash/analytics/*` (ru/kz) | teacher | pass | pass 9 T7: full localized sentences, zero console errors on 20 loads; retention pruning covered by `analytics_rollup` test |
| F56 | kk Intl polyfill | every `/kz` page in Chromium | any | pass | pass 9 N9 + learner9 F06: «2026 ж. 12 қыр.», «66,67», zero hydration errors on 11 pages |
| F57 | public profile courses (anonymous) | `/user/[username]` | anonymous | pass | pass 10 R7: 200 with the public course cards; unknown user → localized not-found |

## Bugs

| id | feature | symptom | root cause | fix commit | verified by critic |
|---|---|---|---|---|---|
| BUG-017 | F23 | "Создать тест" failed on every attempt; then the studio had no "Новый вопрос"; then publish toasted MISSING_MESSAGE. | **fixed+verified** (9359da8, eba48b6): numeric-id guard + v1 create body + partial policy; three query fns sharing one cache key with different shapes; toast key in the wrong namespace. Quiz creation, item authoring, autosave, publish all verified in the browser. | 9359da8 eba48b6 | — |
| BUG-018 | F19 | Course publish blocked on `courses/{id}/readiness` (no v2 route), rendered as a raw "Request failed". | **fixed** (eba48b6): readiness derived from the curriculum as legacy did for its hard rule; per-assessment rules not replicated (QUESTIONS Q-2026-09-11-1). Course published in the browser. | eba48b6 | — |
| BUG-019 | F13 | Learner: a `quiz` activity rendered "Неподдерживаемый тип активности: TYPE_QUIZ"; "Начать" needed a reload; after submit an unlimited-attempts learner never saw the score; exam violations never reached the server (`violation_count=3`, `violations=[]`). | **fixed+verified** (840e991). Quiz taken end to end: start → answer → draft autosave → submit → result card 100% → retake offered. | 840e991 | — |
| BUG-023 | F01 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Login: Enter in the password field does not submit (3 of 4 tries); on a wrong password both fields are cleared; after success the URL stays `/ru/redirect_from_auth`, which 404s on reload and breaks the locale switcher. | critic, pass 3 | pass 3 | needs critic re-drive |
| BUG-024 | F06 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). kz landing: course date badge renders the raw ICU pattern "2026 M09 11" (ru: "11 сент. 2026 г."). The kk formatter has no month names or the wrong locale tag. | critic, pass 3 | pass 3 | needs critic re-drive |
| BUG-025 | F08 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Learner course page lists an UNPUBLISHED activity ("gauntlet video", a builder's draft); opening it hits the error boundary "Не удалось загрузить курс" (server: activity not in course). The in-activity sidebar correctly omits it. | course page builds its outline from `getCourseMetadata(..., withUnpublishedActivities)` or the curriculum without filtering `published` for learners | pass 3 | needs critic re-drive |
| BUG-026 | F08 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Course progress card says "0% · Завершено 0 из 4" while the activity sidebar says "Основные уроки 2/2" and the learner has published submissions; the progress strip and "Обновления курса" card appear late (~40px layout jump). | two different progress sources | pass 3 | needs critic re-drive |
| BUG-027 | F09 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). A learner can open `/ru/collections/new` (button hidden, route open) and submit; the server correctly answers 403 `collection:create:platform`, but the server action turns it into a 500 and the toast says "Не удалось создать коллекцию. Попробуйте снова." | server action swallows the problem+json 403 | pass 3 | needs critic re-drive |
| BUG-028 | F13 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Result review row "Посмотреть ответы": earned-score cell is empty although the API has 100/100; attempt-history timestamp "9/11/2026, 11:04:39 PM" (en-US at ru); ~5 s unlabelled spinner on first open. (The "????" item text is my curl seed, not the app.) | critic, pass 3 | pass 3 | needs critic re-drive |
| BUG-029 | F23 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Studio readiness strip shows English + raw codes ("add at least one item / assessment.empty", "prompt is empty / choice.prompt_missing", …); question-type badge "CHOICE"; publish toast "Этап изменён на PUBLISHED" (raw lifecycle). | readiness `message`/`code` rendered verbatim; kind/lifecycle labels not mapped | pass 3 | needs critic re-drive |
| BUG-030 | F25 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Review workspace: English "CHOICE", "CHOICE SINGLE", "Queued", "Evidence checked", "Draft artifact", "Ready"; "Пояснение editor"; "3 слов" (needs plural); "12/8,000" (en separator). Selecting attempt #1 loads it but the highlight stays on #2; the score input on a published attempt silently ignores typing while "Необходимо оценить хотя бы один элемент" shows; 150 in a "/ 100" field is accepted with no validation. | critic, pass 3 | pass 3 | needs critic re-drive |
| BUG-031 | F26 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Gradebook column subtitle shows raw "QUIZ" (others say "Экзамен"); curriculum row for the quiz has no type chip; course header badge "Требует проверки" while the review tab says "Готов". | kind label mapping missing for `quiz` in three places | pass 3 | needs critic re-drive |
| BUG-032 | F38 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). `/ru/dash/admin` as admin → error boundary "Что-то пошло не так!": `TypeError: runs.filter is not a function` in AIOperationsConsole — `GET ai/admin/runs?days=7` returns a keyset page `{items, next_cursor}`, the UI expects an array. Plus a hydration error. `/dash/users/settings/users` role column shows raw slugs "admin / instructor / user". | v1 shape assumption | pass 3 | needs critic re-drive |
| BUG-033 | F07,F08 | **fixed** (pass 3: c7ea0ca / 80bbe59 / ce4ad41). Course hrefs are `http://localhost:3000//course/…` (empty locale segment — UX-007 confirmed real, not a tooling artifact); header search fires `GET search?q=learner` on the course page from a stale header query; search page title has double spaces; "XP за сегодня" beside "Всего ОП" (mixed terminology, kz too); avatar dropdown renders behind "Новая коллекция" (z-index). | critic, pass 3 | pass 3 | needs critic re-drive |
| BUG-036 | F13 (server, security) | **fixed** (e5d69a2). `GET activities/{id}/assessment` returned `options[].is_correct`, explanations, rubrics, reference solutions and hidden tests to learners — the answer key of an open quiz. Non-authors now get a redacted body. Matching `pairs` still carry the pairing (needs a wire shape; QUESTIONS). | e5d69a2 | needs critic |
| BUG-037 | F35 (server) | `POST ai/qa/{course}/chat` 422s on the AG-UI input the client sends (`unknown field tools`), so Q&A never reaches the provider. | DTO `deny_unknown_fields` without the AG-UI `tools/context/state` members — **fixed** | f938ca9 | — |
| BUG-038 | F35 (contract) | `QaMessage.citations` declared as an object but user turns emit `[]`; the generated client rejects any thread with a question (client parses loosely for now). | server now emits `citations` as an object on every turn (migration 20260912000001) — **fixed** | f938ca9 | — |
| BUG-039 | F16 | Parent post shows "(Отредактировано)" after a reply, a reply after a like; hydration mismatch on relative time ("7 минут назад"). | `edited` derived from `updated_at != created_at` while a trigger bumps `updated_at` on counter writes; no edit signal on the wire → marker removed; relative time renders the absolute date until mounted — **fixed** | 27ea2d5 | — |
| BUG-040 | F18 | Gamification preference toggle: toast "сохранено" but reload shows the old value. | nothing under /dash mounted the provider; the page read a stale sessionStorage cache — server component passes the fresh profile — **fixed**, critic C7 pass | 27ea2d5 | — |
| BUG-041 | F11 | `/ru/trail` shows 0/4 while the course page shows 2/4 — trail runs carry only lesson-type steps; the page must read the progress projection. Landing course card likewise 0%. | trail runs carry only lesson steps; trail + landing card read the learner-state projection — **fixed**, critic C7 pass | 27ea2d5 | — |
| BUG-042 | F10 | Quiz sidebar ✓ while the result is "Не пройдено · 0%" (rule `passed`). | result card scored the latest attempt against a hard-coded 60 while the projector scores the best attempt against the passing score; card shows the projection plus "Последняя попытка" — **fixed**, critic C8 pass | 27ea2d5 | — |
| BUG-043 | F40,F04 | Header/sidebar show username instead of display name after a rename; "Профиль" tab duplicates "Общие"; session list dates en-US; unlabelled 5 s spinner. | display-name chips read the username; duplicate profile tab + 14 dead files removed; dates via `useFormatter`; spinner labelled — **fixed**, critic C9 pass | 27ea2d5 | — |
| BUG-044 | F13,F02,F05 | Learner's media 404 (ORB) — local env only: nothing served `/content/` (**fixed** 7e7437f, setup fact added); `/unauthorized` and the draft-activity page have no `<title>`; draft page offers a learner "В панель управления". | titles added; draft page no longer offers the dashboard to learners — **fixed**, critic C10 pass | 7e7437f 27ea2d5 | — |
| BUG-045 | F19,F26,F07 | Teacher's `/dash/courses` rendered "Courses failed to load" (and the dashboard course summary was empty): every caller hit the legacy `courses/editable/page/{p}/limit/{l}`, 404 in v2. | derived client-side from `GET /courses` + `creator_id` + grants (Q-2026-09-10-2 interim) — **fixed**, critic C1 pass | 9d3c1bb | C1 |
| BUG-046 | F07 | Instructors saw an "Admin Work" queue section and an "Администрирование" tool card that both led to `/unauthorized`. | `hasUsersAccess` (true via `usergroup:read:platform`) gated admin-only surfaces — **fixed** | 9d3c1bb 81d7283 | C2 fail → fixed |
| BUG-047 | F39,F38 | Users-settings tabs keyed off `user:read:platform` (every learner has it) and `usergroup:manage:platform` (instructors lack it); no server-side page gates; sidebar/breadcrumb "Пользователи" dead-ended teachers on the admin directory. | gates mirror `GET /users` / `GET /usergroups`; index lands on the first allowed tab; links point at the index — **fixed** | 09e1205 81d7283 | C3 fail → fixed |
| BUG-048 | F38 | `/dash/admin/users` "Failed to load roles and permissions"; both admin pages on legacy shapes (10 typecheck errors). | rebuilt on the v2 RBAC routes (Q-2026-09-10-6 interim); audit log dropped — **fixed**, critic C4/C5/C6 pass | 0f598ff | C4 C5 C6 |
| BUG-049 | F36,F37,F35 | Course-analysis, submission-analysis and remediation panels rendered blank: v1 field names from untyped fetches. | generated schemas + typed queue/publish; 404 → empty state, other errors → recovery — **fixed** | f9f6932 | e2e 07 |
| BUG-050 | F20 | Teacher saw no rename/publish/delete on activity rows (`activity.can_update` not on the wire); "Code challenge" quick-create 422'd with a raw toast (legacy body). | grants + `creator_id`; v2 `CreateAssessmentRequest` — **fixed** | 35ef9a5 81d7283 | e2e 03 |
| BUG-051 | F21 | Editor autosave indicator vanished and quick exits lost lecture content (store reset on every refetch: `course_<id>` vs bare id); header links sliced bare ids. | `openEditor` normalises ids; `cleanCourseUuid` — **fixed** | 35ef9a5 81d7283 | e2e 03 |
| BUG-052 | F23 | Studio "New question → Choice" dead click (Base UI `MenuItem` has no `onSelect`) and uppercase v1 create body → 422. | `onClick` + `itemBodyToWire` — **fixed** | 35ef9a5 81d7283 | e2e 03 |
| BUG-053 | F13 | Exam action bar labelled the button that opens Submit as "Continue". | `resolvePrimaryButtonLabelKey` — **fixed** | 35ef9a5 | e2e 04 |
| BUG-054 | F25,F23 | Exam review queue dropped the learner display name; studio results view sent `sort_by…page_size` → 400 rendered as "Courses failed to load". | shared `reviewItemFromWire`; shared keyset submissions query — **fixed** | 35ef9a5 | e2e 05/06 |
| BUG-055 | F22 | Browser presigned PUT to RustFS failed ("Network unavailable"): buckets had no CORS. | environment: `extra/storage-cors.json` applied — **fixed** (setup fact) | 5a5ff55 | e2e 04 |
| BUG-056 | F35,F36 | AI runs queued forever locally: no worker process was running (`ashyq worker`). | environment — worker started, setup fact added; draft-mode copy is legacy-faithful Russian regardless of UI locale (noted, not changed) | — | C11 |
| BUG-057 | F08,F29 | Every course page requested `courses/{id}/contributors` (404, console error) and offered an "apply to contribute" button that could only dead-click. | both off until Q-2026-09-10-3 lands — **fixed** | 81d7283 | — |
| BUG-058 | F18,F11,F04,F05 | "XP" hard-coded in four gamification components; 404 page had no title; sessions list showed raw user-agents; absolute `getAbsoluteUrl()` hrefs cost a 307 per click; management cards said "Начать обучение". | catalog unit; hoisted `<title>`; UA sniff; AppLink strips the origin; preview label — **fixed** | 81d7283 2fef2c8 | — |
| BUG-059 | F08 (server, security) | Every learner saw every private/draft course in the catalogue, search and collections, then hit `learner-state → 403` and a 500 behind «Начать курс». | v2 treated `course:read:all` (which every role holds — legacy "browse the public catalogue") as a platform-wide bypass; visibility follows the legacy filter, bypass reserved for `course:update|manage:platform` (DECISIONS 2026-09-12) — **fixed** | 1b2a44f | pass 7 |
| BUG-060 | F13 (server) | An exam awaiting release was badged «Не пройдено» — the projector mapped `Graded` to passed/failed. | `Graded → graded_hidden`, score/pass hidden until `Published` — **fixed** | 68c1af0 | pass 7 |
| BUG-061 | F13 (server) | A returned-for-revision attempt at `max_attempts: 1` could be started but not handed in (403 MAX_ATTEMPTS_REACHED). | `enforce_constraints` now applies the revision lift — **fixed** | 68c1af0 | — |
| BUG-062 | F25 | Exam per-item grading stored ×33.33 values and published «1778%» to the learner. | client sent share-of-100 values where the contract takes points on the item's `max_score` scale; converts on send + server 422 guard — **fixed** | f101e80 68c1af0 | pass 7 |
| BUG-063 | F19,F22 | Course thumbnail upload → server action 500, English toast. | legacy `PUT /courses/{id}/thumbnail`; v2 had the upload purpose but no way to claim it — `thumbnail_upload_id` on PATCH (DECISIONS) — **fixed** | 68c1af0 60dab6c | pass 7 |
| BUG-064 | F19 | Fresh course loaded dirty + invalid; raw valibot message blocked save. | schema required learning outcomes not on the wire; list widget mutated on mount — **fixed** | 60dab6c | pass 7 |
| BUG-065 | F21 | Image block «Загрузить» unclickable (covered by the `fill` preview). | container `relative` — **fixed** (script: 201 via real click) | 60dab6c | pass 7 |
| BUG-066 | F20,F29 | Exam dialog fired `assessments/exam/config` (404); collaboration card raw «Request failed / no such route»; chapter ops silent. | dead legacy calls removed; honest unavailable state; toasts — **fixed** | 60dab6c | pass 7 |
| BUG-067 | F26,F27 | Gradebook: no file-submission column, «Экспорт» navigated to a JSON 404, dead checkboxes; no live updates. | wire gaps (Q-2026-09-12-2 #1–3) with client interims: «н/д» columns, client CSV, 15 s polling — **fixed** | f101e80 | pass 7 |
| BUG-068 | F31,F32,F33 | Analytics rendered wire codes/English prose verbatim; intervention dialog 2233 px, unscrollable, wrong action fired, list stale. | ~120 codes localized, prose rebuilt from `kind`; dialog width/scroll/refresh — **fixed** | f101e80 | pass 7 |
| BUG-069 | F13 | After submit: sidebar/badge stale until reload; `submissions/draft` 404 after every submit; restore banner + «Результат 0%» on a fresh attempt. | learner-course invalidation + `router.refresh()`; draft query gated; banner needs answers — **fixed** | f95697b | pass 7 |
| BUG-070 | F15 | Published file-submission activity without config rendered an error card with a trace id. | localized "not set up yet" state (Q-2026-09-12-2 #9) — **fixed** | f95697b | pass 7 |
| BUG-071 | F01,F02 | Logged-out settings page spun forever; login page rendered for signed-in users; TOTP enrolled state invisible after reload and re-enrol 503. | layout `requireSession()`; login redirect; Zitadel `AlreadyExists` → 409 + client flips to enrolled — **fixed** | 1b2a44f f95697b | pass 7 |
| BUG-072 | F39 | Teacher member picker empty (`GET /users` 403); admin picker offered link AND unlink on every row; hydration error on the usergroups page. | picker on `/search` ∪ members; one action from membership; `isPending` vs persisted cache — **fixed** | f95697b | pass 7 |
| BUG-073 | F37 | Submission analysis printed a raw key; «Разбор отзыва»/«План доработки» re-ran the analysis; raw «low/high»; silent publish. | key added, dead commands removed, enums localized, toasts — **fixed** | f95697b | pass 7 |
| BUG-074 | F11,F17,F07,F10 | «Покинуть курс» without confirm/toast; «Скачать сертификат» opened the verify page; pagination `href="#"`; double «Далее». | confirm + toast; relabelled (no download route); disabled edges; one primary — **fixed** | f95697b | — |
| BUG-075 | F06 | `/kz/dash*` carried Russian titles; certificate type «Completion» in ru/kz. | localized title template per section; type enum localized — **fixed** | 0092732 f95697b | pass 7 |
| BUG-076 | F14 | Code challenge cannot be authored or run locally: Judge0 absent (`code/languages` 503). | environment — **blocked** (setup fact; e2e skips on the probe) | — | — |
| BUG-077 | F08,F11 (server) | «Покинуть курс» removed the trail run but the learner stayed enrolled at 25 % and could not re-enrol. | projection kept; legacy deleted the steps — leave now resets lesson completions, enrolment derived from real progress (DECISIONS 2026-09-12) — **fixed** | 581a06c | — |
| BUG-078 | F13 (data/ops) | Six unreleased exams still projected `passed/failed` after 68c1af0. | persisted rows are only rewritten on the next write — `ashyq admin progress-backfill` after projector changes (DECISIONS note) — **fixed** (backfill run locally) | — | pass 7 |
| BUG-079 | F25 (data) | Five e2e-graded exams carried ×33.33 item scores (published «3333 %»). | rows written before the client scale fix; grading entries are append-only, so each was re-graded through the API with correct points — **fixed** (local data) | — | — |
| BUG-080 | F26 | Gradebook file-submission cells were «н/д» for a published 77 % file grade; «FILE SUBMISSION» raw label. | the wire has no file cells (Q-2026-09-12-2 #1); the query now walks each file activity's attempts and projects them — **fixed** | 1b1a3ad | pass 8 |
| BUG-081 | F31,F32 | kk KPI definitions English; «cutover/backlog/scope» in ru/kk; raw `published`; kz hydration page errors from Intl under Chromium-without-kk-ICU. | catalogs; `suppressHydrationWarning` on SSR'd Intl leaves — **fixed** | 1b1a3ad | pass 8 |
| BUG-082 | F21,F14 | Studio logged a React `flushSync` error on lectures with image blocks; an unconfigured code challenge showed a trace-id error card and a dead «Начать». | `setContent` in an effect constructs node views synchronously → microtask; localized not-configured state — **fixed** | 1b1a3ad | pass 8 |
| BUG-083 | F02,F07,F35 | Base UI uncontrolled-field warning on the TOTP step; kk `<time>` hydration mismatch in the work queue; Q&A delete toast/dialog wording; contributors intro contradicted its state; finding review silent. | fields keyed per step; suppressed leaf; copy; toast — **fixed** | 1b1a3ad | — |
| BUG-084 | F47 (server) | A `user`-role maintainer got 403 on every curriculum write and `/unauthorized` on the workspace — authorship still required `course:update:own`. | active roster row IS the `:own` scope in every authoring predicate; reporters excluded from `contributor_ids` — **fixed** | d40554c | R1 |
| BUG-085 | F48 | Publishing a file-submission activity with a draft config → server-action 500 + generic toast (409 lost across the action boundary, BUG-035 pattern). | curriculum mutations call the API from the client; blockers link to their stage — **fixed** | 6167ef4 | R2 |
| BUG-086 | F13,F15 | «Черновик восстановлен» appeared on a brand-new attempt after the first autosave; file submit left the header/footer stale until reload. | "had draft on mount" snapshot; shared `refreshLearnerCourseState` after file submit — **fixed** | 6167ef4 | R3 R5 |
| BUG-087 | F50,F51 | Learner result page showed no per-item verdicts and mixed number formats; matching UI was a select per term with a raw `Matching.correct` key in review. | verdicts from `feedback_code`, one formatter, two-column matching UI (duplicate choice-module path deleted), keys ×3 — **fixed** | 6167ef4 | R3 R4 |
| BUG-088 | F54 | Link preview lookup fired twice, logged a page error and kept `href="not a url"`. | one request per URL, `mutate` not `mutateAsync`, `<a>` only for http(s) — **fixed** | 6167ef4 | R6 |
| BUG-089 | F47 (server) | Active reporter on a private course: landing rendered over `learner-state → 403`. | `user_has_course_access` accepts any active roster row — **fixed** | 0946a2a | — |
| BUG-090 | F54 | A saved block whose URL failed validation re-fetched (422 + error toast) on every learner view. | previews resolve only in an editable editor for http(s) URLs; the dialog validates the scheme inline — **fixed** | 0946a2a | — |
| BUG-092 | F41 (server) | Signup with a password that fails the Zitadel policy (e.g. `abcdefg1`) answers **503 service-unavailable** and the page shows «Сервис временно недоступен»; the account is not created. | Zitadel policy error (code 3, `COMMA-VoaRj`) was not mapped in `zitadel.rs::create_human`; now 422 field `password`/`password-policy` (localized rule in ru/kk/en) — **fixed** | 6db7895 | critic11 identity |
| BUG-093 | F41/F01 (server) | `users.email` uniqueness and the login lookup are case-sensitive: `C11ID19114@EXAMPLE.TEST` registered a second account next to `c11id19114@example.test`, and login with the email/username in another case → 401 (register rejects the username case-insensitively). | `find_user_for_login` / `find_user_id_by_email` compared exact strings and `users.email` had only a case-sensitive UNIQUE; now `lower()` on both sides, emails stored lower-cased at creation (register/admin/Google/ETL), migration `20260913000001` adds unique `lower(email)` + `lower(username)` indexes; the dev duplicate `c11id19114d` was deleted (users, user_roles, Zitadel 390559229095707909) — **fixed** | 40c640d | critic11 identity |
| BUG-094 | F42 (server) | Changing the password to the current password → 503 (`zitadel change password failed: … COMMAND-CahN2`) instead of a 422 field error. | Zitadel answers new == current as code 13 `COMMAND-CahN2` (verified live); `zitadel.rs::change_password` now maps it to 422 `new_password`/`password-unchanged` (policy rejections → `password-policy`) — **fixed** | 6db7895 | critic11 identity |
| BUG-095 | F04 (server) | `GET /auth/sessions` bumps `last_seen` and the idle TTL of **every** listed session (`list_sessions → session_peek → get_and_touch`), so idle/stolen sessions never idle out while the owner views the page, and every row shows the same «Последняя активность». | `list_sessions` (and `logout`) went through `get_and_touch`; new `SessionStore::peek` reads without SET/EXPIRE — **fixed** | 414db96 | critic11 identity |
| BUG-096 | F02 (server) | Login with a `totp_code` for an account that is not enrolled → 503 `zitadel session check failed: Multifactor…` instead of 400/401. | Zitadel code 9 «Multifactor OTP isn't ready» (`COMMAND-3Mif9s`) fell into the 503 arm of `create_password_session`; with a `totp_code` it is now `InvalidTotp` → 400 `invalid-totp-code` — **fixed** | 6db7895 | critic11 identity |
| BUG-097 | F40 (server) | `PATCH users/me {"display_name":""}` → 200 and the header renders an empty name. | `UsersService::update_my_profile` now trims `display_name` and rejects a blank one with 422 `display_name`/`required` — **fixed** | f5c9438 | critic11 identity |
| BUG-098 | F08/F11 (server) | A learner can `POST trail/activities/{id}` for an **unpublished** activity → 200; it becomes a required `activity_progress` row (`total_required_count` 3→4) that survives unmark/leave/re-enrol; with all published ones done the web says «Курс завершён 3 из 3» while the server says `in_progress 75 % 3/4` — never `certificate_eligible`, no completion XP. Teacher unpublish/delete leaves 3/4 until the next write. | `TrailService::add_activity` had no `published` guard and `list_course_progress_rows` returned every row. Now: marking a draft → 404 `not-found`; the aggregate reads published rows only (`activity_id IN (SELECT id FROM activities WHERE published)`); activity (un)publish and delete re-aggregate every known learner (`recalculate_course_for_all`) — **fixed** | — | critic11 learner-course |
| BUG-099 | F08/F10 (server, permission) | `GET activities/{id}` returns the full draft `content` of an **unpublished** activity to a learner AND to anonymous; `GET courses/{id}/curriculum` lists it with `published:false`. The web hides it (BUG-025 was UI-only); the API leaks. Legacy: `activity:read` unless public && published; curriculum filtered unless `with_unpublished_activities`. | no editor check on unpublished reads in `catalog/curriculum.rs`. Now `readable_activity` (activity detail, blocks, block by id) and `curriculum` gate drafts on the authoring check (`CoursesService::require_write` = creator / active contributor / platform updater): learners and anonymous get 404 / a filtered curriculum — **fixed** | — | critic11 learner-course |
| BUG-100 | F11 (server) | `DELETE trail/courses/{unknown}` and `{private course}` → 200. | no existence/visibility check in `TrailService::remove_course` — now `courses.get` first (404 unknown/invisible, as on the way in) — **fixed** | — | critic11 learner-course |
| BUG-101 | ALL (server) | Malformed query/path (`limit=abc`, `cursor=xyz`, `/courses/not-a-uuid`) → plain-text axum rejection («Failed to deserialize query string…») instead of problem+json 400/422. | axum default `Query`/`Path` rejections bypassed `ApiError`. Now `crates/api/src/extract.rs` owns `Query<T>`/`Path<T>` newtypes whose rejection is 422 `validation-failed` with a `query` / `path` field error (same shape as `ValidJson` for `body`); every route imports them (auth.rs/users.rs pending — another builder holds those files) — **fixed** | — | critic11 learner-course |
| BUG-091 | F08 (server) | After leaving a course with submissions the learner stayed `enrolled:true` (rows past `not_started`), so «Начать курс» could never re-enrol. | enrolment = trail run (legacy `TrailRun`); the projector creates the run on the first submission — **fixed** | d40554c | R8 |
| BUG-034 | F18 (server) | `PATCH gamification/preferences {"privacy":{"showOnLeaderboard":false}}` is stored but the learner still appears in `GET gamification/leaderboard`. The opt-out is not applied server-side. | **fixed** (f938ca9): leaderboard query filters opted-out profiles | f938ca9 | — |
| BUG-035 | ALL (arch) | Fourteen `'use server'` service files are called from client components; `APIError.code` does not survive the Next server-action boundary in production, so problem+json codes degrade to the generic localized fallback (and a 403 once surfaced as a 500, BUG-027). Client-side calls should use `apiJson` directly (the pattern used by the fixed collections and auth flows). | architectural; per-service conversion | — | — |
| BUG-020 | F25 | Teacher review queue spun on "Загрузка…" with "0 работ" while the header counted 2; then 400 on the list. | **fixed+verified** (64be288, f510686): queries gated on a legacy numeric `activityId` the v2 review passes as 0; the list still sent v1 offset params. Also `GET assessments/undefined/attempt-state` from the grader's inspector borrowing the learner hook, and three shapes on one cache key. Grade published in the browser; learner then saw the released score. | 64be288 f510686 | — |
| BUG-021 | contract | `openapi.v2.json` had one dangling `$ref` (`ReviewStatus`, a query-only enum utoipa does not collect). | **fixed** (95404c1) — registered explicitly, snapshot refreshed, zero dangling refs. | 95404c1 | — |
| BUG-022 | F15,F22 | Video/PDF activities streamed multipart through the API on the v1 chunked protocol; file submissions were v1 end to end (numeric ids, `course_id`, ISO dates, unquoted If-Match, no idempotency, offset paging). | **fixed** (8419e87) — `uploadFile()` → presigned PUT → `POST activities/{id}/blocks`; file submissions on the generated types with quoted If-Match, Idempotency-Key and keyset review paging; every route curled live. Browser verification of the file-submission flow is pending (F15). Left: the Tiptap video block still uses the v1 chunked uploader; video subtitles have no v2 upload purpose. | 8419e87 | — |
| BUG-016 | ALL admin surfaces | **fixed.** Every server-side permission check silently denied an **admin**. | `AUTH_PERMISSION_WILDCARD` (apps/web/src/lib/auth/types.ts:30) is `'*'`, and `sessionCan` (apps/web/src/lib/auth/permissions.ts:17) tests `perms.has('*')`. The server's super-admin grant is the literal string **`*:*:*`** — verified by logging in as admin@ashyq.local, whose `permissions` array is exactly `["*:*:*"]`. So the wildcard branch never matches and an admin only passes checks for permissions listed explicitly, which for a `*:*:*` account is none. Affects every `sessionCan` / `requirePermission` / `requireAnyPermission` caller, and the client-side `can()` in `components/providers/session-provider.tsx` has the same bug. `Users.tsx` and the new course-capability helper each work around it locally with an ad hoc `.includes('*:*:*')`. Fix: one constant, then delete both workarounds. | — | — |
| BUG-015 | F19–F29 (whole teacher surface) | **fixed+verified** (ce5ddd9; workspace overview and curriculum render for the teacher). Every stage of the teacher course workspace — overview, details, curriculum, gradebook, access, collaboration, certificate, review — renders "Курс не найден" for a course the teacher owns. | Not `getCourseMetadata` (that is correct v2 and returns 200). `renderCourseWorkspacePage` also awaits `requireCourseWorkspaceStageAccess` → `getCourseWorkspaceCapabilitiesForCourse` (apps/web/src/lib/course-management-server.ts:68) → `getCourseUserRights`, which calls the v1 route `courses/{id}/rights`. That route does not exist in v2, answers 404, and the workspace's catch turns 401/403/404 into the not-found page. v2 has no rights endpoint at all and `CoursePermissions` is a learner-facing discover/access/enroll schema used by no path. The capabilities are derivable with no contract change: the session already carries the RBAC grant strings and `GET courses/{id}` carries `creator_id` for `:own` scoping. | pending | — |
| BUG-013 | F09 | Submitting "Создать коллекцию" with no course selected does nothing at all — no validation message, no toast, no field highlight. The button looks broken. Selecting a course makes the same click succeed. | not yet investigated (apps/web/src/app/[locale]/(platform)/(withmenu)/collections/new). The requirement itself may be right; the silence is the bug. | — | — |
| BUG-014 | F09 | The course picker's checkbox does not respond to a click on the checkbox itself, only to a click on the course title. `form_input` on the input element also leaves the count at "Курсы не выбраны". | not yet investigated — likely the visually-hidden input is not wired to the label, so the hit target is the title only | — | — |
| BUG-011 | F19 | (**fixed, but the page still 404s for a second reason — see BUG-015.**) The teacher course workspace showed "Курс не найден / Запрашиваемый курс не существует или был удален" for a course that exists. Repro: as teacher, `/ru/dash/courses/new` → name → Create; the course IS written to the DB, then the workspace claims it is missing. `/ru/dash/courses/<real id>` reproduces it directly. | `getCourseMetadata` (apps/web/src/services/courses/courses.ts) calls the v1 route `courses/{id}/meta`, which the v2 server answers 404 `not-found`. `GET courses/{id}` (200) and `GET courses/{id}/curriculum` (200, `{chapters: []}`) are the v2 replacements. Part of the drift map. | — | — |
| BUG-012 | F19 | The "Стартовый шаблон" option on the course-create form promises "две готовые главы: Введение и Основные уроки" and silently creates none. Verified: after creating with it selected, `courses` has the row and `chapters` is empty. | `useCreateCourseMutation` sends `template: 'starter'` to `createNewCourse`, which drops it — v2 `CreateCourseRequest` is `{name, description?, about?, tags?}` with `additionalProperties: false` and has no template concept. Only the `copy-outline` branch actually seeds chapters client-side (via `createChapter`, which IS correct v2); `starter` has no such branch, so it silently degrades to `blank`. | — | — |
| BUG-009 | F04 | **fixed+verified** (header dropdown → "Выйти" → login page, session gone; coordinate click at native size). Originally: the dashboard sidebar "Выйти" button was a dead click: two clean attempts at a real coordinate (1280x800 viewport, `/ru/dash` as learner), 8 s wait, no navigation, no console error, and a full page reload still shows the session. Server-side logout is correct in isolation: `POST /api/v2/auth/logout` answers 204, clears the cookie and turns the next `GET /auth/session` into 401. The page IS hydrated (the locale switcher works). | Not fully pinned. Strong lead: `DashSidebar.tsx:189` calls `logout({ redirectTo: getAbsoluteUrl('/login') })`, i.e. an ABSOLUTE url, and `normalizeReturnTo` (src/lib/auth/return-to.ts:18) rejects anything not starting with `/` and returns `'/'`. `/login` is also an auth route, which `normalizeReturnTo` maps to `'/'` anyway — so `getAbsoluteUrl` here is pointless as well as wrong. That explains a wrong redirect target but not the total no-op; needs a builder with the dev-server action log. | — | — |
| BUG-010 | F06 | In the kk locale the header nav keeps two Russian strings: the nav landmark's accessible name stays "Навигация", and the "Прогресс" nav item is untranslated. **Still open in pass 11** («Прогресс» on every /kz screen, kz-home.png). | not hard-coded: the kk catalog carried Russian for `Components.NavMenu.navigation`, `Components.NavMenuLinks.trail`, `General.trail`, `Components.Breadcrumb.label/ariaLabel` — now «Шарлау» / «Үлгерім» (matches `TrailPage.title`) — **fixed** | — | critic11 learner-course |
| BUG-006 | F38 | A learner (role `user`) saw a "Пользователи" entry in the dashboard sidebar and mobile nav, and the page it opened rendered the full admin directory UI — heading "Управляйте пользователями, назначайте роли и разрешения" — with the user list showing "Пользователи не найдены." | `canSeeUsers` (apps/web/src/lib/rbac/navigation-policy.ts) gated on `user:read:platform`, which EVERY learner holds for reading public profiles. The directory behind it is `GET /users`, which the server gates on `platform:read:platform` and correctly answers 403 for a learner (verified per role: learner 403 / teacher 403 / admin 200). So there was no data leak — but the nav advertised a surface the role cannot use, and the page rendered the 403 as an empty result set. Nav gate fixed; the 403-as-empty-state is BUG-007. An existing test asserted the buggy behaviour and was inverted. | pending | — |
| BUG-007 | F38 | `/ru/dash/users/settings/users` renders a 403 from `GET /users` as "Пользователи не найдены. / 0 видимых строк" — a permission denial presented as an empty result. | not yet investigated (apps/web/src/components/Dashboard/Pages/Users/Users/Users.tsx) | — | — |
| BUG-008 | F07,F28 | **fixed+verified.** The dashboard work queue rendered in English in ru/kz: "Learner Work", "Assignments and course actions…", "No learner work is queued", plus an English "Unavailable" status badge. | `apps/web/src/features/work-queue/dashboard-work-queue.ts` builds the whole view model from ~89 hard-coded English literals; only the individual items are localized, by `localizeWorkItem` in dash/page.tsx. Also `aria-label="Go to first page"`/`"Go to last page"` in the shared data-table pagination and `aria-label="Copy code"`. | pending | — |
| BUG-005 | ALL | `request_id` is documented in the problem+json envelope (ARCHITECTURE §5, apps/web/AGENTS.md) but never reaches the wire. | `crates/api/src/error.rs:76` hard-codes `request_id: None` and nothing populates it; `ApiError::into_response` runs inside `PropagateRequestIdLayer` and cannot see the id. The web client falls back to the `x-request-id` header, so impact is limited. | — | see QUESTIONS.md §5 |
| BUG-004 | ALL client-side | Every browser-side v2 call fails; the security page's session list spins on "Загрузка..." forever. Console: "Request header field traceparent is not allowed by Access-Control-Allow-Headers in preflight response". | `cors_layer` (apps/server/crates/api/src/app.rs:452) allowlisted only content-type/accept/x-request-id/last-event-id/idempotency-key. The web client stamps `traceparent` on EVERY request (apps/web/src/lib/api-client.ts:230,297) and `If-Match` on locked writes, and the layer also exposed no response headers, so a cross-origin caller could never read `ETag` (the new version after a locked write) or `x-request-id`. Invisible in production only because nginx puts web and API on one origin. | pending | — |
| BUG-002 | F07 | Home page rendered the "Курсы временно недоступны" degraded state for every user. | `fetchCourses` (apps/web/src/services/courses/courses.ts) called the v1 URL `courses/page/{p}/limit/{l}`; the v2 server answered `no such route` and LandingContent's catch-all returned `LandingDegradedState`. | **fixed** (builder, pass 1) — the landing page now renders the real profile card, stats and course sections. Needs a critic. | pending |
| BUG-003 | ALL | **downgraded — not a build blocker.** `next dev` logs Cache Components errors (E1429/E1432/E1437/E1439) on nearly every route. A production `next build --debug-prerender` (with the contract-blocked admin type errors temporarily ignored) generated all 208 pages with every route ◐ Partial Prerender and **zero** blocking-prerender failures. These are the dev-only instant-navigation validations the adoption skill describes, worth a sweep for navigation speed, not a shipping defect. The one real item — `randomHex` calling crypto during server render — is fixed. | dev-only validation | 36f6cc2 | — |
| BUG-001 | F38 | No way to create a user in v2. Self-registration is deliberately out (apps/web/AGENTS.md "Not in v2", and the login page says accounts are created by the platform admin) — but there is no **admin** user-creation route either: openapi.v2.json has no `POST /users`, so after cutover accounts can only appear via `ashyq admin etl` / `zitadel-import`. Legacy had `POST /users` (apps/api/src/routers/users.py:60). | contract gap, no DECISIONS entry | — | see QUESTIONS.md |

## UX notes

| id | feature | what felt wrong | decision |
|---|---|---|---|
| UX-015 | F41/F42 | Password hint «Не менее 8 символов.» / «Кемінде 8 таңба.» on signup + security while Zitadel demands upper+lower+digit+symbol. | **fixed** — `passwordRule` (signup + security) and the admin `passwordHint` state 8+ chars, upper, lower, digit, symbol in ru/kk/en (a3da691) |
| UX-016 | F41 | verify-email: after a wrong code the form action resets the uncontrolled inputs, the email is wiped and the second submit fails with «Обязательно» on email. | **fixed** — verify-email keeps `values` in the action state and re-mounts the form per submit (same pattern as signup) (dfba1de) |
| UX-017 | F42/F04 | After a successful password change the sessions list stays at the old rows until reload while the toast says «Остальные сессии завершены». | **fixed** — `invalidateQueries(queryKeys.auth.sessions())` in the password mutation's `onSuccess` (a3da691) |
| UX-018 | F02 | «Отключить» two-factor removes TOTP immediately with only a toast; revoke-session has a confirm dialog. | **fixed** — AlertDialog before `removeTotp` (`disableTotpConfirmTitle/Description`, ru/kk/en) (a3da691) |
| UX-019 | F02 | TOTP step → «Назад ко входу» is a dead click: URL becomes `/ru/login` but the same `LoginClient` keeps `step: 'totp'`; only a reload shows the login field. | **fixed** — the back link is a `intent=back` submit that resets the action state to the credentials step (keeps the login) (dfba1de) |
| UX-020 | F01 | `Retry-After: 60` on the login-name limit while the window is 900 s. | **fixed** — identity limiters read the key's Redis TTL into `details.retry_after_seconds`; `Retry-After` on 429 uses it (60 s fallback elsewhere) (414db96) |
| UX-021 | F08 | Anonymous course page → «Начать курс» lands on `/ru/auth/signup` with no returnTo; every anonymous course view also logs two client-side `learner-state` 401s. | **fixed** — «Начать курс» / «Подать заявку» for anonymous → `buildLoginRedirect(/course/{id})` (login with returnTo, desktop + mobile); the landing learner-state query was already session-gated — the observed 401s were the activity page's server-side auth gate, which now checks the session cookie before any fetch and redirects to login |
| UX-022 | F08 | Course landing heading «Авторы и обновления» — only «Обновления курса» renders; no author/contributor block since BUG-057. | **fixed** — `CourseAuthors` renders the active roster from `GET courses/{id}/contributors` (creator first; signed-in only, the endpoint is 401 for anonymous) under «Авторы и обновления»; the heading is omitted when there is nothing to show |
| UX-023 | F47 | A pending applicant cannot withdraw the application: `DELETE contributors/{self}` → 403 and the UI shows only «…на рассмотрении». | **fixed** — a pending applicant may `DELETE contributors/{self}` → 204 (DECISIONS 2026-09-13); «Отозвать заявку» (ru/kk/en) under the pending badge on the landing |
| UX-024 | F11 | ru trail «Учебная задача выполнено» (gender). | **fixed** — «Учебная задача выполнена» plus the same gender slips on the other «Учебная задача …» strings (отправлена / оценена / сохранена / должна) |
| UX-001 | F01 | The password reveal control on the login page had the English accessible name "Show password" while the rest of the page was Russian. | **fixed** — `src/components/ui/custom/password-input.tsx` now uses `t('showPassword')`/`t('hidePassword')`; keys added to all three catalogs. Verified in the browser: "Показать пароль" (ru), "Құпия сөзді көрсету" (kz). |
| UX-002 | F04 | With the API unreachable the session list showed an unlabelled spinner for well over a minute (react-query retried 7+ times with backoff) instead of reaching its error state. The error branch exists and is correct; the retry policy is what hides it. | fix — cap retries on this query so the existing `sessionsLoadError` + retry button appears within a few seconds |
| UX-003 | F02 | The two-factor section renders "Включить двухфакторную аутентификацию" and "Отключить" side by side for an account with no TOTP enrolled. "Отключить" is a dead click. | fix — render the enable/disable control from the account's actual enrolment state |
| UX-004 | F38 | (promoted to BUG-006 — it was a real gate defect, not a judgement call.) | — |
| UX-009 | F13 | **fixed+verified** (fe56d86: badge derives from the effective policy; learner now sees "Использовано 2 из 3 попыток"). Was:  Activity header badge says "Неограниченное количество попыток" while the card says 1 attempt (exam with max_attempts=1). | fix |
| UX-010 | F13 | **fixed** (fe56d86). Was:  Attempt view leaks English: raw kind label "quiz" above the title, "1 answered · 1 / 1" in the footer, "50 min" time limit; dnd-kit's "To pick up a draggable item, press the space bar…" announcer in the studio; result card "Учебная задача отправлено" (needs "отправлена"). | fix |
| UX-011 | F23 | builder could not reproduce blanks (dialog renders all values in jsdom); "Затронутые учащиеся" now omitted until its count loads. Re-check in the browser next pass. Was:  Exam publish confirmation lists "Затронутые учащиеся / Вопросы / Баллы / Попытки" with no values — only the time limit is filled. | fix |
| UX-012 | F13 | **fixed** (fe56d86: bundle requested only when the session can edit the course). Was:  The learner activity page fetches the teacher editor bundle (contributors/usergroups/certifications → 403/404) on every load. Wasted calls and console noise. | fix — gate on capability |
| UX-013 | F26 | Gradebook default filter and counters ignore graded-but-unreleased submissions (batch release mode): "На проверке 0", row hidden until "Все". The teacher's next action — release the batch — is invisible. | fix |
| UX-014 | F25 | Review workspace English leaks: "No answer provided", "Awaiting release", "SLA breaches", "Sep 11, 10:59 PM" dates. | fix |
| UX-007 | F08 | The course page breadcrumb links to `http://localhost:3000//courses` — a doubled slash. It still resolves, but the href is malformed. | fix — a path is being joined to a base that already ends in `/` |
| UX-008 | F08 | A course with no chapters renders the heading "Уроки курса" followed by nothing — no empty state telling the teacher to add a chapter. | fix |
| UX-006 | F07,F30 | Header nav and search-result links render without the locale prefix (`/courses`, `/course/{id}`), so each one takes a 307 through the middleware to `/ru/...` instead of navigating client-side. The dash sidebar renders the same kind of link WITH the prefix. Both use `AppLink` → next-intl `Link`, so the difference is unexplained. Functionally correct, costs a round trip. | investigate — a critic should settle whether the DOM href really lacks the prefix or this is an artifact of how the accessibility tree reports resolved URLs |
| UX-005 | F38 | The admin page title flashes ("Панель администратора") before the permission guard redirects a learner to `/unauthorized`. Cosmetic; no data renders. | accept — metadata resolves before the guard; not worth a render-blocking guard |
